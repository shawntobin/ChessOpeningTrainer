class Square {
  constructor(id, rank, file, color, piece, seqnnum) {
    this.id = id;
    this.rank = rank;
    this.file = file;
    this.color = color;
    this.piece = piece;
    this.seqnnum = seqnnum;
  }
}

export default Square;
