class Square {
  constructor(id, rank, file, color, piece, occupied) {
    this.id = id;
    this.rank = rank;
    this.file = file;
    this.color = color;
    this.piece = piece;
    this.occupied = occupied;
  }
}

export default Square;
