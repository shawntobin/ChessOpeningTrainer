class OpeningSummary {
  constructor(id, name, moves, category, image) {
    this.id = id;
    this.name = name;
    this.moves = moves;
    this.category = category;
    this.image = image;
  }
}

export default OpeningSummary;
